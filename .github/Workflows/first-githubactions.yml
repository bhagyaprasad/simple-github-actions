name: Run shell commands
on: [push]

jobs: 
  run-shell-command:
   runs-on: ubuntu-latest
   steps:
    -name: Echo a string
    run: Echo "Hello Bhagya!! This is our first GITHUB actions Workflow file"
